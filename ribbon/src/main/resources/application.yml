server:
  port: 8010
spring:
  application:
    name: consumer-ribbon
eureka:
  client:
    healthcheck: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
  instance:
    preferIpAddress: true
