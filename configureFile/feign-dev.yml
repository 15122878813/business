server:
  port: 8765
eureka:
  client:
    healthcheck: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: consumer-feign
#启用hystrix断路器
feign:
  hystrix:
    enabled: true
