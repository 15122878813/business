spring:
  profiles:
    active: dev
---
server:
  port: 8765
spring:
  profiles: dev
  application:
    name: consumer-feign
  cloud:
    config:
      label: master
      uri: http://127.0.0.1:9005
      profile: dev

